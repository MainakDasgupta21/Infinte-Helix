// MeetingTimeline — Vertical list of today's meetings from Google Calendar
// Props: { meetings: [{ id, title, startTime, endTime, attendees }] }

import React from 'react';

export default function MeetingTimeline({ meetings }) {
  // TODO: Vertical timeline with time markers
  // TODO: Highlight next upcoming meeting
  return <div>MeetingTimeline</div>;
}
