// ScreenTimeChart — Donut chart showing today's screen time breakdown
// Uses: react-chartjs-2 Doughnut
// Props: { totalMinutes, activeMinutes, idleMinutes }

import React from 'react';

export default function ScreenTimeChart({ totalMinutes, activeMinutes, idleMinutes }) {
  // TODO: Chart.js Doughnut with helix color palette
  return <div>ScreenTimeChart</div>;
}
