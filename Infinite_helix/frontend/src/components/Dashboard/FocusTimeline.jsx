// FocusTimeline — Line chart showing focus intensity throughout the day
// Uses: react-chartjs-2 Line
// Props: { sessions: [{ startTime, endTime, focusScore }] }

import React from 'react';

export default function FocusTimeline({ sessions }) {
  // TODO: Chart.js Line with gradient fill
  return <div>FocusTimeline</div>;
}
