// NudgeFeed — List of recent wellness nudges delivered today
// Props: { nudges: [{ id, type, message, timestamp, dismissed }] }

import React from 'react';

export default function NudgeFeed({ nudges }) {
  // TODO: Scrollable list with nudge type icons
  return <div>NudgeFeed</div>;
}
