// BreakBalance — Progress bar showing breaks taken vs recommended
// Props: { taken, recommended }

import React from 'react';

export default function BreakBalance({ taken, recommended }) {
  // TODO: Animated progress bar with gradient
  return <div>BreakBalance</div>;
}
