// PhaseSelector — Cycle phase selection UI
// Phases: menstrual, follicular, ovulation, luteal
// Props: { currentPhase, onSelect }

import React from 'react';

export default function PhaseSelector({ currentPhase, onSelect }) {
  // TODO: Four-card horizontal selector with phase-colored borders
  // TODO: Active state animation
  return <div>PhaseSelector</div>;
}
