// WeeklyInsight — AI-generated weekly wellness insight card
// Props: { insightText, suggestions: string[] }
// Example: "You focused deeply this week but skipped breaks on Wednesday."

import React from 'react';

export default function WeeklyInsight({ insightText, suggestions }) {
  // TODO: Gradient-bordered card with AI icon
  return <div>WeeklyInsight</div>;
}
