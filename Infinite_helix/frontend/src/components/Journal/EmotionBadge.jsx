// EmotionBadge — Displays detected emotion from AI analysis
// Props: { emotion, confidence }
// Emotions: joy, sadness, anger, fear, surprise, disgust, neutral

import React from 'react';

export default function EmotionBadge({ emotion, confidence }) {
  // TODO: Colored badge with emoji + label
  // TODO: Confidence percentage indicator
  return <div>EmotionBadge</div>;
}
