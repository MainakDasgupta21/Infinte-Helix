// EntryHistory — Timeline of past journal entries
// Props: { entries: [{ id, text, emotion, sentiment, timestamp }] }

import React from 'react';

export default function EntryHistory({ entries }) {
  // TODO: Vertical timeline with emotion-colored dots
  // TODO: Expandable entry cards
  return <div>EntryHistory</div>;
}
