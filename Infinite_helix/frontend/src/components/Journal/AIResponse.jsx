// AIResponse — Displays AI-generated supportive message
// Props: { message, emotion, type }
// Types: reframing, affirmation, suggestion, encouragement

import React from 'react';

export default function AIResponse({ message, emotion, type }) {
  // TODO: Card with soft gradient background
  // TODO: Different styling per response type
  return <div>AIResponse</div>;
}
